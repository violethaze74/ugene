<multi-test>

    <!-- Test reading file: a name in an RD tag doesn't mutch with a name in an AF tag -->

    <import-broken-document url="apr/MOMP2(error).apr" outUrl="apr/MOMP2(error)_" io="local_file" format="Vector_nti_alignx"/>

</multi-test>